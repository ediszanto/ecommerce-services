server:
  port: 3000

spring:
  application:
    name: eureka
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id: true

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
